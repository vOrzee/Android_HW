<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main"
    tools:context=".activity.MainActivity">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:layout_editor_absoluteX="0dp"
        tools:listitem="@layout/post_card" />

    <androidx.constraintlayout.widget.Group
        android:id="@+id/edit_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:constraint_referenced_ids="edit_pencil, edit_constant, edit_preview, edit_cancel"
        android:visibility="invisible"/>

    <ImageView
        android:id="@+id/edit_pencil"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_edit_24"
        android:padding="8dp"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@android:color/white" />

    <TextView
        android:id="@+id/edit_constant"
        android:layout_width="0dp"
        android:layout_height="16dp"
        android:text="@string/edit_constant"
        android:textAlignment="textStart"
        android:textColor="#00aaff"
        android:background="@android:color/white"
        app:layout_constraintStart_toEndOf="@+id/edit_pencil"
        app:layout_constraintTop_toTopOf="@id/edit_pencil"
        app:layout_constraintEnd_toStartOf="@id/edit_cancel"/>

    <TextView
        android:id="@+id/edit_preview"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Привет, это новая..."
        android:padding="2dp"
        app:layout_constraintTop_toBottomOf="@id/edit_constant"
        app:layout_constraintStart_toEndOf="@id/edit_pencil"
        app:layout_constraintEnd_toStartOf="@id/edit_cancel"
        android:background="@android:color/white" />

    <ImageView
        android:id="@+id/edit_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="8dp"
        android:src="@drawable/baseline_close_24"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/edit_constant"
        android:background="@android:color/white"
        />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/baseline_add_24"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="@dimen/common_spacing"
        />


</androidx.constraintlayout.widget.ConstraintLayout>